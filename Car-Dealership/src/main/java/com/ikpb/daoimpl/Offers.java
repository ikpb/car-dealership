package com.ikpb.daoimpl;

import java.util.List;

import com.ikpb.pojo.Car;
import com.ikpb.pojo.User;
import com.ikpb.users.Employee;

public class Offers {
	private double amount;
	private int id;
	private Car car;
	private Customer employee;
	
	public List<Offers>makeOffer(User user, Car car, int offer ) {
		if(car.offers) {}
	}
	public void makeOffer(String cust, int number, int carId) {
		for(int i=0;i<cars.size();i++) {
			if(car == (cars.get(i).getId())) {
			
			}
		}
	}
}
